load ../../../contrib/tools/itp/itp-tool.maude
load ../../../contrib/systems/data-structures/tree.maude

select ITP-TOOL .
loop init-itp .
(goal leaves-inner :
  TREE |- A{T:Tree}
    ((leaves(T)) = (s(inner(T)))) .)

(ind on T:Tree .)
(auto .)
(auto .)
